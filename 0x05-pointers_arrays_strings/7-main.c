#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;
    char *nam;

    str = "0123456789";
    nam = "Oluwalogbon";
    puts_half(str);
    puts_half(nam);
    return (0);
}
